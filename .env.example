# Local Ollama — routes ADK/LiteLLM to your downloaded Gemma models
OLLAMA_API_BASE=http://localhost:11434

# Model tag in Ollama (must match `ollama list`)
OLLAMA_MODEL=gemma4:e2b

# Optional: only needed if you run cloud evals or deploy with Gemini
# GEMINI_API_KEY=your_key_here
